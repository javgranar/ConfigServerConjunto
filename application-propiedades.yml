server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javgranar/ConfigServer
          default-label: main
          search-paths:
          - '*propiedades'
